//vector.h
class Point{
public:
	double x,y;
};
class Vector{
public:
	Point start, end;
	int distancia(Vector vec1.end.x, vec1.start.x, vec1.end.y, vec1.start.y){
		int result;
		result=sqrt(pow(((vec1.end.x)-(vec1.start.x)),2)+pow(((vec1.end.y)-(vec1.start.y)),2));
		return result;
	};
class Circulo{
public:
	float radio;
	float area(Circulo rad){
		int areacirculo;
		areacirculo=Pi*pow(rad.2);
		return areacirculo;
	}
};
